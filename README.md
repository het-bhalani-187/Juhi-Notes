This app was vibe coded in 3:22 AM and the only purpose this app has is to act as a bridge between me and my co-founder so that we can share our thoughts seemlessly, and we are aware of other alternatives but they just dont fit the box either they ask for money or they are not very convinent to use. Dhanyawad!!✌️

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
   
3. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
   
5. Run the app:
   `npm run dev`

   IK It's ODD😂
